services:
  db:
    image: postgres:16-alpine
    container_name: TaskMaxing
    environment:
      POSTGRES_DB: TaskMaxing
      POSTGRES_USER: TaskMaxing
      POSTGRES_PASSWORD: TaskMaxing
    ports:
      - "5435:5432"