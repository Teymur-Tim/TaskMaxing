server:
  port: 3333
spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/TaskMaxing
    username: TaskMaxing
    password: TaskMaxing

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
